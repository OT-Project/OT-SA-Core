###################################################################
# Local syslog-ng configuration filter definition [sshd].
# Captures SSH session access logs from the sshd daemon and audit
# events emitted by the SSH settings page (e.g. host-key regenerate).
###################################################################
filter f_local_sshd {
    program("sshd") or program("sshd_audit");
};
